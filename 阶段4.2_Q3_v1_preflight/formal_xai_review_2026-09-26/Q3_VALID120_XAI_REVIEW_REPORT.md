# Q3 冻结模型 valid120 解释验证审核报告

日期：2026-09-26。审核对象是 B0_seed2029 在预冻结附件2 valid120 子集上的解释验证；本报告不评价 test，也不包含附件4正式推理。

## 1. 输入与运行身份

| 项目 | 核对结果 |
|---|---|
| 服务器结果 ZIP SHA-256 | `33ef931cfacfee17ce36746b975e4d0440a7aec5371ddb616ad32193876b6645`，与回收记录一致；ZIP CRC通过 |
| 运行源码 | 冻结运行包提交 `a6b6bb552613cd4c59ab23e99f15b5b8a920a082` |
| 模型与设备 | B0_seed2029；Python 3.12.14、torch 2.8.0+cu128、numpy 2.5.0；CUDA GPU 1 |
| 原数据身份 | `aligned_50.pkl` SHA-256 `66e867aa74bc70a844e806e5571e371c9abb4a35f9e2887ce9b4d97ff2cb8fcd` |
| 样本 | 冻结 ID 清单顺序完全一致；120 个唯一 valid 样本，84 个 video_id 组 |
| 输出 | 120 个逐样本 JSON；输出 manifest 129 个文件逐一哈希通过；另有 manifest 和两份日志，共132个打包文件 |
| 边界 | `optimizer_steps=0`、`test_evaluated=false`、`attachment4_inference=false`；stderr 为空 |

审核脚本从逐样本数据重新计算 Shapley 加和、IG 完备性、有效位置和 padding、扰动位置与随机对照数量、18组 video_id 分组 bootstrap 区间、24组敏感性汇总。结构审计为 `PASS`，失败项 0。服务器曾按 float32 求随机扰动均值；审核中保持相同精度，避免将至多 `5.7×10⁻⁸` 的重算差视为实验错误。原始结果未修改。

## 2. 数值与特征空间扰动证据

正式运行状态为 `FORMAL_XAI_COMPLETE`，120/120；4080/4080 数值检查通过，epsilon重复性检查通过。两个输出目标×三个模态×120样本，共720个局部状态为 `localized_feature_position`，没有数值失败或强制补出的 Top-k。

下表展示固定规则中替代20%有效位置时，“IG所选位置的绝对输出变化减去50组等数量随机位置的平均绝对变化”。区间是按84个 video_id 分组、2000次配对 bootstrap 得到的95%区间。

| 目标 | 模态 | 平均差 | 95%区间 |
|---|---|---:|---:|
| 分类概率 | 文本 | 0.08266 | [0.07158, 0.09421] |
| 分类概率 | 语音 | 0.00677 | [0.00576, 0.00782] |
| 分类概率 | 视觉 | 0.00523 | [0.00402, 0.00665] |
| 回归输出 | 文本 | 0.11500 | [0.09395, 0.13663] |
| 回归输出 | 语音 | 0.01755 | [0.01508, 0.02041] |
| 回归输出 | 视觉 | 0.01260 | [0.00999, 0.01560] |

10%、20%、30%三档共18个预定组合的区间下界均大于0，逐项报告见服务器 `perturbation_summary.json`。这些组合共享样本与位置，且未作多重比较校正，不能把18项当作独立的总体显著性证明。分类概率与回归输出的数值单位不同，不能直接比较两列作用强度。分类正向支持位置另用有符号概率下降复核，未用绝对变化冒充“支持”。

## 3. 参考、种子和随机化敏感性

将 train 均值参考改为 train 中位数参考后，主要作用模态标签在分类115/120、回归117/120个样本上一致；分类主要支持模态在116/120个样本上一致。下表是有效样本的局部位置重要性排序 Spearman 均值；比较目标类始终固定为原 B0_seed2029 的完整输入预测类。

| 目标/模态 | 中位数参考 | seed2030 | seed2031 | 参数随机化 |
|---|---:|---:|---:|---:|
| 分类/文本 | 0.992 | 0.720 | 0.624 | 0.063 |
| 分类/语音 | 0.962 | 0.424 | 0.481 | 0.128 |
| 分类/视觉 | 0.971 | 0.533 | 0.381 | 0.037 |
| 回归/文本 | 0.986 | 0.672 | 0.459 | -0.015 |
| 回归/语音 | 0.970 | 0.386 | 0.274 | 0.014 |
| 回归/视觉 | 0.966 | 0.317 | 0.179 | 0.051 |

视觉每目标有5个样本的重要性位置并列，Spearman未定义；对应视觉行的分母是115，其余行是120。种子间稳定性尤其语音和视觉较弱，论文中只能报告固定交付模型的特征位置归因及其敏感性，不宜声称局部位置跨训练种子高度稳定。参数随机化后排序接近零相关，说明排序依赖已训练参数；这项检查仍不构成因果验证。

三模态 Shapley 在本 valid120 子集的主要作用模态计数：分类文本107、视觉10、语音3；回归文本92、视觉19、语音9。这是相对冻结模型、参考输入和目标函数的描述，不是现实情绪信息的因果份额。

## 4. 解释范围与审核决定

附件2 valid 的 text/audio/vision 三种原素材映射状态均为 `index_only`。本结果支持**固定 B0 模型在预定 valid120 子集上的 feature-space attribution 和特征位置扰动忠实性**；不支持把语音索引写成秒数、视觉索引写成视频帧，也不支持在 valid 上直接声称词语证据。附件4文本20条样本的 `verified_text` 范围来自独立的 C-4 证据，不能外推到这里。

**本轮审核决定：`VALID120_FEATURE_SPACE_REVIEW_PASS_WITH_LIMITATIONS`。** 可准备下一个冻结步骤，但当前运行文件的 `final_gate=REVIEW_GATE` 必须原样保留；审核决定应另行记录。尚未完成的是：附件4解释卡固定选样数量和样本规则的操作化、附件4全部20条正式预测与解释、可选的一次性 test 评价、4.5内部结果检查、4.6独立红队及复验、4.7最终冻结。本轮尝试调用独立审核代理时遇到工作区额度不足，因此**没有获得本轮120条结果的独立代理审查结论**；不得将准备包的P1/P2验收误写成本轮结果的独立审查。

当前阶段汇报：①完成120条正式运行结果的完整结构和统计复核；②数值通过，预定扰动对照在特征空间提供正向证据，但种子稳定性混合；③原音视频精确映射、附件4输出及独立红队仍未完成；④满足进入下一冻结步骤的技术条件，尚未自动跨阶段。
