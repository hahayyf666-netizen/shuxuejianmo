# C-4 独立只读核查回执

核查范围：附件4 `01–20` 的 C-4 代码、结果、来源 SHA 和当前 Git 变更；核查者不修改工件、不执行 Q3 训练或推理。

| 核查项 | 结果 |
|---|---|
| 文本输入链 | `raw_text` 重放 tokenizer 后，ID/attention/segment 与官方 `text_bert` 完全一致；再执行固定 BERT 第12层前向与官方 `text` 对比。 |
| 全量文本追溯 | 独立核对20个 aligned PKL SHA、raw_text SHA、564条官方行 SHA 和字符 offset，未发现异常。 |
| 文本数值复核 | 564/564 行在每维 `≤1e-4` 容差下通过；独立重跑样本20的43个内容行也全部通过。该结论不是 bitwise 相等：四位小数后768维全部相等的只有3/564行。 |
| 音频 | 候选25D与官方74D不同，0/3677官方行获得严格验证。CKA中位数约0.345，倒序约0.109；不能据此声称时间窗映射。 |
| 视觉 | 候选52D与官方35D不同，0/2593官方行获得严格验证。16条可比样本CKA中位数约0.538，倒序约0.195；样本01、13、20无有效候选脸，16仅1个官方有效行。 |
| 变更边界 | 既有Q3协议与模型追踪文件未见改动；未见Q3预测器加载、训练或推理。 |

**审核结论：**文本在本批数据上可标 `verified_text`（逐维容差恢复）；音频与视觉保持 `index_only`。技术备选可冻结为 feature-space attribution；现行 Q3 原始证据与正式训练门仍为 STOP。媒体零行只表示本次**零行已严格验证**，不证明将来无法恢复。
