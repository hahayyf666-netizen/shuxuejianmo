# 2.1.7 冻结三条 A0 pilot 执行报告

**当前状态：REVIEW GATE。** 三条冻结样本均已按流水线处理并生成审计材料；结构审计通过。普通样本有一个零时长词，按协议标记为 `SAMPLE FAIL`；音频内容人工核对尚待审核，因此本报告不判定 A0 pilot 整体通过，也不进入 2.2。

## ① 完成了什么

- 样本清单保持原样，SHA-256 为 `3aea2949e059ba0b8d7cb94c5b50872f76a450c99af62c58414455ef03ea67eb`。只处理：

  | sample_key | 角色 | 输出词数 | 结果 |
  | --- | --- | ---: | --- |
  | `-s9qJ7ATP7w$_$0` | ordinary | 24 | `SAMPLE FAIL`：第 4 个输出词 `they` 的对齐区间为 `[0.780, 0.780)`；保留该词与位置，不伪造边界。其余 23 词完成三模态特征。 |
  | `-mJ2ud6oKI8$_$6` | edit-list | 5 | `COMPLETED`：5/5 词具备有效对齐、文本、音频和视觉特征。 |
  | `-s9qJ7ATP7w$_$6` | edit-list | 5 | `COMPLETED`：5/5 词具备有效对齐、文本、音频和视觉特征。 |

- 官方文本来自 `label-100.xlsx` 的 `text` 列。没有调用 `transcribe()`、`transcribe_stable()` 或其他 ASR，也没有把模型转写替换为标签文本。
- 使用稳定版本组合：Python 3.12.10、stable-ts 2.19.1、openai-whisper 20250625、torch 2.8.0+cpu、openSMILE 2.6.0、MediaPipe 0.10.35。设备为 CPU，CUDA 不可用。
- stable-ts 调用为 `model.align(audio_16k_mono, official_text, language="en", remove_instant_words=False, failure_threshold=None, regroup=False, stream=False)`。
- RoBERTa 固定为 `FacebookAI/roberta-base`，revision `e2da8e2f811d1448a5b465c236feacd80ffbac7b`；`model.safetensors` SHA-256：`5bde1d28afb363d0103324efeb5afc8b2b397fe5e04beabb9b1ef355255ade81`。MediaPipe 官方 `face_landmarker.task` SHA-256：`64184e229b263107bc2b804c6625db1341ff2bb731874b0bcc2fe6544e0bc9ff`。Whisper `base.en.pt` SHA-256：`25a8566e1d0c1e2231d1c762132cd20e0f96a85d16145c3a00adf5d1ac670ead`。
- 三种逐词特征维度均符合冻结定义：RoBERTa 768；eGeMAPSv02 的 25 个 LLD 均值与总体标准差合计 50；MediaPipe 的 52 个 blendshape 均值与总体标准差合计 104。NPZ 中另存原始 LLD、帧 PTS、视觉支持区间、每词底层帧索引、字符跨度、有效性掩码、缺口和源文件哈希。
- 每条样本均生成 NPZ、cache、对齐 TSV/JSON trace、16 kHz 单声道试听 WAV、manifest 行和 stdout/stderr/退出码日志。输出 NPZ 可在 `allow_pickle=False` 下读取，不含情绪目标标签；每条序列长度与 valid_length 相等，无补齐或删词。
- 首次执行时复用一个 MediaPipe VIDEO 实例导致后续样本时间戳重置报错。已将其归类为 `RECOVERABLE / CONTINUE`：每条样本各用一个新实例，版本、模型、VIDEO 模式和 PTS 毫秒时间语义均不变；完整重跑后三条结果均已写出。证据与首轮日志见 `recovery_log.json` 和 `logs/attempt_1/`。

## ② 得到什么结论

- 三条样本的音频、视频首个解码呈现 PTS 均为 `0`，共同原点 `t0=0` 可确定。解码帧 PTS 严格递增；所有归词使用半开区间 `[start,end)`，不以帧序号除以名义 FPS 代替真实 PTS。
- 两条 edit-list 样本的实际 presentation 覆盖与音频覆盖不同，均在同一时间轴记录：

  | sample_key | 视频呈现覆盖 | 16 kHz 音频覆盖 | 视觉有效帧比例 |
  | --- | ---: | ---: | ---: |
  | `-mJ2ud6oKI8$_$6` | 0–2.257 s | 约 0–2.2281 s | 1.000 |
  | `-s9qJ7ATP7w$_$6` | 0–2.475 s | 约 0–2.3148 s | 0.831（59/71 帧） |

- 普通样本容器/视频轨声明 6.800 s；PyAV 解码的最后视频帧 PTS 为 6.8333 s，按该帧实际时长得到支持结束 6.8667 s；音频覆盖约 6.7106 s。两项时长差异保留在日志与 NPZ 中，归词仍按解码 PTS 执行。普通样本末段有两个 66.7 ms 帧间隔；`-s9qJ7ATP7w$_$6` 有一个 133.3 ms 间隔；未插帧或重写时间戳。
- 普通样本 24 个词全部保留，映射成功 24/24；但一个词区间为零时长，因此 `alignment_valid=0`，该词的音频/视觉特征置零且对应 mask 为 0。此样本的音频、视觉有效词数分别为 23/24；没有静默删词，也没有为它均匀分配时间。
- 机器结构审计结果为 `PASS`：三条样本顺序、来源哈希、序列长度、922 维分模态形状、raw 特征维度、掩码关系、NPZ 可读性和无标签检查均通过。最终 runner 退出码 0、用时约 46.1 s；样本结束时进程 RSS 约 1.83 GiB。pilot 输出目录共 1,230,552 bytes（含首轮恢复日志）。这只是工件与结构审计通过，不等于时间边界质量或模型效果通过。
- 未触发 HARD STOP。stable-ts 的 Whisper 兼容性提示来自本地安装包的 `direct_url.json` 检测；实际 `openai-whisper` 版本为 20250625，安装包哈希与资格门记录一致，因此保留提示、不改冻结调用。RoBERTa 加载提示中的 pooler 未参与逐词表示；特征仅取最后隐层。

## ③ 还有什么未确认

- 需要人工逐条播放 `review_audio/` 中的三段音频，对照 `alignment/` 中的官方文本和词时间。forced alignment 不能单独证明音频确实说了这些词；本次没有把这项内容核对写成已通过。
- 请重点复核普通样本第 4 词 `they` 的零时长边界，以及两条 edit-list 短样本的文本与实际音频是否相符。对齐 trace 保留工具原始词、原文字符跨度和时间，可追溯检查。
- 目前只完成这三条 pilot；没有运行其余 97 条，也没有进入 Q2/Q3。pilot 证据不支持最终模型路线优劣结论。

## ④ 是否满足进入下一阶段的条件

**不满足；按协议停在 REVIEW GATE，等待本次结果审核。** 普通样本的一个零时长词尚未解决，且人工内容核对尚未确认。当前不进入 2.2，不改样、不扩样。

## 审核材料索引

- 汇总：`run_summary.json`、`manifest.csv`、`artifact_audit.json`、`environment_and_assets.json`
- 三条试听：`review_audio/-s9qJ7ATP7w___0.wav`、`review_audio/-mJ2ud6oKI8___6.wav`、`review_audio/-s9qJ7ATP7w___6.wav`
- 对齐词表：`alignment/*_words.tsv`；字符跨度和完整映射：`alignment/*_trace.json`
- 逐样本记录：`logs/*.report.json`、`logs/*.stdout.log`、`logs/*.stderr.log`；首轮恢复证据：`logs/attempt_1/`
- 资产哈希：`environment_and_assets.json`；可复核恢复说明：`recovery_log.json`
