# Q1 第一版功能闭合验收报告

日期：2026-09-24  
阶段：Q1 步骤09；保持 2.1.7 REVIEW GATE  ️
验收状态：**作者侧门禁通过；独立 P2 只读验收 PASS**  
范围：闭合问题1第一版功能、100条特征交付、异常保留逻辑、可迁移安装与复现材料；不进入2.2，不开始最终全题附件组装。

## 结论摘要

本轮修复了原题功能检查指出的两个 P1 复现缺口：固定依赖不再引用原电脑绝对路径；稳定时间词对齐的上游实现、转词映射代码、输入权重校验和执行命令已随包交付。使用迁移解压目录和干净 CPython 3.12.10 CPU 环境完成安装验收、九条异常/代表分支复现和五条已有词级trace回放；全量100条文件验证及需求重审均通过。最终归档重建后另对该归档做了全量结构和需求重审。

**Q1第一版功能与交付材料已达到作者侧闭合条件。** `UNCERTAIN_REVIEW` 表示该条没有可主张的词级文本—音视频映射，实际按片段级文本和共享 A/V 时间序列保留；它不是要求用户逐条听辨的待办状态。100条无提示 ASR 屏查已有逐条结果，计数为 close 62、moderate 16、high 18、empty 2、digital silence 2。ASR只作为机器证据，不被写成全量人工真值；不改官方文本、不移除样本，也不伪造词时间。

## ① 完成了什么

### 原题要求交付

- 保留附件1全部100个官方样本键；100个正式 NPZ、100行 `manifest.csv`、100行 `results_100.csv` 一一对应。官方文本仍逐字来自 `label-100.xlsx`，输入MP4和标签表均有SHA-256记录。
- 文本使用固定revision的 RoBERTa-base 最后隐层；音频使用真实MP4音轨及 openSMILE eGeMAPSv02 25维LLD；视觉使用真实视频帧PTS及 MediaPipe FaceLandmarker 52维blendshape。原生序列保留各自有效长度，文本词、音频窗、视频帧不强行补成同一长度。
- 词时间使用半开区间与共同 presentation timeline；只对有对应依据的词聚合音频/视觉，保存中心索引、mask和CSR来源索引。样本级关联则保留官方文本序列与原生A/V序列，不制造词边界。
- 正式异常路径可用：确认数字静音2条仍保留音频观测，`audio_speech_valid=0`；24条无可用面部blendshape仍保留视频帧，`video_present=1`、`face_feature_valid=0`；5条已确认文本—音频不匹配使用 `AV_VALID_TEXT_UNALIGNED`，保留官方文本和实际A/V，不生成错误词时间。
- 典型映射验证、方法说明、字段合同、统一读取器、日志、数据和模型资产清单、便携安装入口及依赖锁均在候选包中。

### 复现和验收

- **干净安装：PASS，退出码0。** 从迁移解压包在新的CPython 3.12.10环境安装固定CPU依赖，`pip check`通过；PyTorch为 `2.8.0+cpu`，CUDA不可用；stable-ts、openai-whisper和模型权重的版本/哈希来源写入安装回执。
- **全量100条结构与原题需求复审：PASS，退出码0。** 100/100样本检查通过、无样本键缺失或多余；manifest与results逐字节一致；100个NPZ均可无pickle读取；所有A0保护文件和清单哈希不变。
- **九条冻结分支迁移复现：PASS，9/9。** 最终候选归档解压目录上，第一次因0.92 GB可用内存低于1 GiB预检阈值而在运行前暂停；内存恢复后以相同冻结样本及参数重试，完整特征逐数组比较 `rtol=1e-6, atol=1e-6` 全部通过。完整恢复运行日志和逐样本比较结果位于 `reports/final_archive_recheck_2026-09-24/`。
- **五条已有词级trace复算：PASS，5/5。** 在候选包解压目录及干净CPU环境中使用冻结 stable-ts 2.19.1、base.en、原官方文本、共享presentation-time解码及保存的转词映射逻辑；trace、时间字段、索引、mask以及已保存派生特征均匹配。逐条回执位于同一复验目录。
- **候选ZIP校验：PASS。** 实际压缩包大小、50,000,000-byte预算余量和SHA-256见本次生成的 `package/q1_closed_size_report.json` 与 `package/q1_closed_archive.sha256` sidecar。压缩包成员清单包含100个NPZ及全量媒体审计证据；ZIP CRC、成员集合与SHA-256均通过。Q2/Q3和其他参赛材料尚未合并，因此该数值只表示Q1包体积，不表示全题预算已验收。

安装和复现的原始stdout、stderr、退出码、pip freeze、安装来源报告、模型哈希与逐样本回执位于本目录 `reports/`。重新执行命令见同目录 `README.md` 及候选包中的主 `README.md`。

## ② 得到什么结论

| 检查项 | 结果 |
|---|---|
| 原始样本覆盖 | 100/100；无漏项、重项或额外特征文件 |
| text/audio/video模态存在 | 各100/100；音视频共同presentation时间轴100/100可解释 |
| 输出文件 | 100个NPZ、100行manifest、100行汇总表；manifest与汇总表字节相同 |
| 映射模式 | `TRI_MODAL_WORD_VALID=5`；`AV_VALID_TEXT_UNALIGNED=5`；`TRI_MODAL_WITH_AUDIO_CONTENT_INVALID=2`；`UNCERTAIN_REVIEW=88` |
| ASR内容筛查 | `CLOSE_BY_HEURISTIC=62`；`MODERATE_DISAGREEMENT_REVIEW=16`；`HIGH_DISAGREEMENT_REVIEW=18`；`ASR_EMPTY_REVIEW=2`；`DIGITAL_SILENCE=2` |
| 可信词级映射 | 71个官方词时间区间；音频派生有效71词、视觉派生有效68词，3个视觉观测缺口由mask明确表示 |
| 旧A0保护 | 48/48基准哈希通过；迁移重审中131个保护文件无变化 |
| 便携复现 | 干净CPU安装通过；9条完整流水线样本和5条词级trace均通过，首轮资源暂停及成功重试日志保留 |
| Q1候选包 | 实测大小及SHA见package sidecar；CRC/成员哈希通过；全题附件总量尚待Q2/Q3完成后计算 |

本题要求是给出合理、可复现的三模态特征提取与时序组织模型，并提供覆盖完整、可核验、可复现的特征文件；并未要求所有100条都必须获得逐词级跨模态真值。对真实数据中的文本/音频内容差异、数字静音、无脸和对齐器失败，保留官方输入及实际信号并显式标注其可用层级，符合已提供的官方答疑口径。

## ③ 还有什么未确认

- 88条 `UNCERTAIN_REVIEW` 的文本—音频语义关系不被声称为逐词确认；它们已经完成全量ASR机器屏查，特征输出按片段级文本与A/V时间序列保留，不需要用户逐条听辨才能闭合Q1。
- ASR筛查旗标并非人工真值，也不用于估计全量转写错配率；报告保留其机器证据等级，不把ASR输出替换为官方文本。
- 本轮在干净迁移包中执行的是九条固定分支复现和五条词级trace复算；没有再重复提取全部100条三模态向量。原始100条生产文件已通过全量核验，提交包中提供固定代码、环境、模型来源、hash与命令。
- 最终全题50 MB预算要在Q2/Q3及所有附件汇总后另行核算。

以上都是结果边界或后续阶段事项，不是Q1当前缺失的特征文件、manifest或可执行异常分支。

## ④ 是否满足进入下一阶段的条件

作者侧Q1步骤09门禁与独立P2只读验收均通过，未发现阻止Q1 v1闭合的缺口。P2回执见`reports/P2独立只读验收_2026-09-24.md`。**本轮不自动进入2.2。** Q1 v1功能交付现已闭合；仍按阶段协议停在2.1.7 REVIEW GATE，等用户授权启动2.2“检查与改进”。Q1第一版闭合不等同Q1最终冻结。最终重建归档SHA及复验状态以包外回执为准。

## 证据索引

- `reports/full100_validation/`：100条结构检查、原题需求审计、逐样本结果与输入快照。
- `reports/nine_branch_reproduction/`：迁移目录9条完整复现命令、日志、manifest、NPZ及比较结果。
- `reports/five_trace_replay_migration/`：五条官方文本词级trace的独立回放逐条JSON及总表。
- `reports/portable_env_clean/`：干净安装原始报告、pip freeze、安装receipt和两个经过SHA验证的源码分发包。
- `reports/final_archive_recheck_2026-09-24/`：最终候选归档重建前后的只读复审记录、9条冻结分支恢复重试及5条词级trace精确回放日志与逐样本结果。
- `code/q1_requirement_alignment_audit.py` 与 `code/q1_repro_contract_audit.py`：随包交付的只读独立复审脚本。
- `package/`：最终候选ZIP清单、体积报告和ZIP SHA-256 sidecar。
- `../reports/requirement_alignment_check_2026-09-24/`：初始原题复审和已修复问题的历史记录；本报告记录修复后的复验结论。
